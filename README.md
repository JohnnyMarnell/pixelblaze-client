# docs-assets

Images referenced from pull request descriptions and docs. Kept on an orphan
branch so a 500 KB GIF is not part of any code diff.

- `docs/images/sensor-sound.gif` — `pb sensor sound` drawing the 32 sensor bins
  it is streaming, with LUFS loudness. Captured on a 110x20 terminal from
  `pb sensor sound --file "Phone Down.m4a" --from 1:30 --to 1:42 --fps 40`.
